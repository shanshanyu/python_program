
python 中的运算符：

()   每个语言里面都有，优先级都是最高

[]   索引运算符，访问列表元组的值，也可以做一些切片

.   属性访问，可以用来访问对象的属性，python中一切皆对象


**   乘方，这个运算符 c 和 java 中都没有



+  -   *   /   %   //   ：加减乘除、取余     整除在 java 和 c 中也没有,在python中 2/3 会得到一个小数，也就是从整数运算后会得到小数。但是 java 和 c 中不会，在java和c中，数据的类型是固定的，2/3 只会得到 0.   整除也只有在 python 里面有，c 和 java 都没有，c 和 java 中 // 是单行注释。


<<   >>   左移   右移


>  >=   <   <=   ==   !=     大于   大于等于   小于  小于等于   等于  不等于，比较


&   ^   |     按位于   按位异或    按位或


and  or   not    逻辑与   逻辑或   逻辑非 ,python 中的逻辑运算符和 c、java 中的不一样，c 和java是 && ||   !


=   赋值运算符,python 中不支持符合运算符，c 和 java 中支持

,  逗号运算符,c 和 python 中有 , 运算符，但是两个效果不一样


