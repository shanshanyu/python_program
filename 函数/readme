函数是一段可以重复执行的代码。

函数定义：
def 函数名称(x,y):
    xx;
    [return [返回值]];


python 中的函数没有返回值，可以返回任意类型的值。


函数参数有：位置参数    关键字参数   普通参数收集    关键字参数收集


还有逆向参数收集，可以把列表、元组、字典传递给函数参数。  //在 ssh 脚本中把 ssh 的连接参数传递给了 connect



函数可以嵌套函数，这一点在 c 和 java 中都不行。
嵌套函数的函数叫做 封闭函数。
被嵌套的函数叫做 局部函数。


